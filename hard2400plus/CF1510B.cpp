#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int D , N;
    cin >> D >> N;
    for(int i = 1; i <= N; ++i){
        string t; 
        cin >> t;
    }
    
}